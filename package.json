{
  "name": "recursive-json-key-transform",
  "version": "0.1.0",
  "description": "Recursively converts object keys according to specified transform function within JSON-compatible object.",
  "main": "dist/index.js",
  "scripts": {
    "build": "mkdir -p dist && babel src/index.js --out-file dist/index.js",
    "dev": "mkdir -p dist && babel src/index.js --watch --out-file dist/index.js"
  },
  "keywords": [
    "recursive",
    "json",
    "objects",
    "arrays",
    "keys",
    "transform"
  ],
  "author": "Ben Wiley",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.8.0",
    "babel-preset-es2015": "^6.6.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/benwiley4000/recursive-json-key-transform.git"
  },
  "bugs": {
    "url": "https://github.com/benwiley4000/recursive-json-key-transform/issues"
  },
  "homepage": "https://github.com/benwiley4000/recursive-json-key-transform#readme"
}
